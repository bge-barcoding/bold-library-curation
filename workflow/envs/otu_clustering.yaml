name: otu_clustering
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - perl=5.32.*        # Less restrictive
  - perl-dbi
  - perl-dbd-sqlite
  - vsearch=2.22.1     # Keep specific version for reproducibility